/*
****** ES7 new features
*/

// Includes `.includes()`
const pets = ['Dog', 'Cat', 'Sheep']
console.log(pets.includes('Sheep'))

// Exponentiation operator `**`
const powerOf = (x, y) => x ** y
console.log(powerOf(56, 5))
